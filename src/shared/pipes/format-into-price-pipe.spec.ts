import { FormatIntoPricePipe } from './format-into-price-pipe';

describe('FormatIntoPricePipe', () => {
  it('create an instance', () => {
    const pipe: FormatIntoPricePipe = new FormatIntoPricePipe();
    expect(pipe).toBeDefined();
  });
});
